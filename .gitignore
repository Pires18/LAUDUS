# Dependencies
node_modules/

# Build output
dist/
build/

# Environment
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cache
.cache/
.parcel-cache/
.vercel

# PACS / Orthanc worklist files (nunca commitar arquivos de pacientes)
*.wl
# Exports de dados clínicos gerados por operações de admin (contêm PHI — nunca commitar)
clear-clinical-findings-report-*.json
# Pastas geradas com caminho Windows literal no macOS/Linux (qualquer caixa).
# NÃO usar barra invertida — no gitignore ela é escape e quebra ferramentas
# (rg/glob); as variantes com '/' cobrem o mesmo caso.
C:/
[Cc]:/[Oo]rthanc*
.env*
